{
    "KafkaSettings" :{
        "Topic" :"quickstart-events",
        "ConsumerSettings" : {
            "bootstrap.servers" : "localhost:9092",
            "group.id" : "consumer-app"
        }
    }
}